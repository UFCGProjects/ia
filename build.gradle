subprojects {
    apply plugin: 'application'
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    dependencies {
        testCompile 'junit:junit:4.11'
    }

    version = '0.1.0'

    jar {
        manifest.attributes provider: 'gradle'
    }
}